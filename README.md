## Prérequis
Ce projet dépend du module `user-service-spi`, généré depuis le contrat OpenAPI.
Avant de builder ce projet, cloner et installer le SPI :

    git clone https://github.com/TON-COMPTE/user-service-spi.git
    cd user-service-spi
    mvn clean install